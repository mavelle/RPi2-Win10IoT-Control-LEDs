<Page
    x:Class="LED.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LED"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        
        <StackPanel Width="200" VerticalAlignment="Center" HorizontalAlignment="Left">
            
            <Button Name="redBtn" Content="Red" HorizontalAlignment="Stretch" Click="redBtn_Click" />
            <Button Name="yellowBtn" Content="Yellow" HorizontalAlignment="Stretch" Click="yellowBtn_Click" Margin="0,20" />
            <Button Name="greenBtn" Content="Green" HorizontalAlignment="Stretch" Click="greenBtn_Click" />
            
            <Button Name="clearBtn" Content="Off All" HorizontalAlignment="Stretch" Click="clearBtn_Click" Margin="0,50" />
        </StackPanel>

    </Grid>
</Page>
